## Notes on RWKV-7

This implementation is based on the [RWKV-7](https://github.com/BlinkDL/RWKV-LM) repository.Make sure the path environment `HEAD_SIZE` and the config `head_size`(nanoLMengine/configs/model_configs/rwkv7_base.yaml) are set to 64. 